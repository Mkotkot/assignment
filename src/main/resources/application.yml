spring:
  datasource:
    url: jdbc:h2:mem:test_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: root
    password: root
  tomcat:
    max-wait: 20000
    max-active: 50
    max-idle: 20
    min-idle: 15
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        id:
          new_generator_mappings: false
    open-in-view=true:server: true
  port: 8081 # This is the default port anyway, but you can change it here

fixedDelay:
  in:
    milliseconds: 20000
demo:
  body1: First Templet Hi how can i help you
  body2: Second Templet do you still avaibale
  body3: Third templet please let me know how can help you
  reply: Thank you for your email the issue was solved

